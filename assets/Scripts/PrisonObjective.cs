using UnityEngine;
using System.Collections;

public class PrisonObjective : PrisonObject 
{
	// Use this for initialization
	void Start () 
	{
		//this shit is glowing
		//Destroy certain object
		//detect itself for oncollision enter
		//Make script detect the object it is attached to - make the W,H the same as the glowing Sphere.
	}
}
